#include "utils/factory.h"

// template <typename T>
// Glib::RefPtr<T> factory::create(guint xid, Glib::ustring& instance_name, Glib::ustring
// group_name, WnckWindowType wintype)
//{
// return Glib::RefPtr<T>(new DockItem(xid, instance_name, group_name, WnckWindowType wintype));
//}
